<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "BlackLotus" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag '/favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <span class="brand"><abbr title="{T}, Sacrifice Black Lotus: Add three mana of any one color to your mana pool.">BlackLotus</abbr></span>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li class="<%= menu_item_active?('home') %>"><%= link_to "Home", root_path  %></li>
              <li class="<%= menu_item_active?('cards') %>"><%= link_to "Cards", cards_path  %></li>
              <li class="<%= menu_item_active?('decks') %>"><%= link_to "Decks", decks_path  %></li>
              <li class="<%= menu_item_active?('editions') %>"><%= link_to "Editions", editions_path  %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span2">
          <div class="well well-small sidebar-nav">
            <% if Rails.env == 'development' %>
              <p><%= image_tag 'http://placehold.it/251x204/888888/000000&text=Bl', :class => 'img-rounded', :alt => 'Bl Development'%></p>
            <% else %>
              <p><%= image_tag "black_lotus.jpg", :class => 'img-rounded' %></p>
            <% end %>
            <ul class="nav nav-tabs nav-stacked">
              <li><%= link_to "Add Card", new_card_path %></li>
              <li><%= link_to "Add Deck", new_deck_path %></li>
              <li><%= link_to "Utilities", utilities_path %></li>
            </ul>
          </div>
          <div>
            <%= yield(:sidebar) %>
          </div>
        </div>
        <div class="span10">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div>

      <p></p>
      <footer>
        <p class="muted well well-small text-center">&copy; Craig N. Caroon 2013</p>
      </footer>

    </div>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= yield(:javascript) %>

  </body>
</html>
